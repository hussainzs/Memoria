---
agent: agent
---
1. Use absolute imports for all internal module references within the `src` directory.
2. If you need to run any terminal commands, ensure to activate the virtual environment first using `& C:/Code/memoria/backend/.venv/Scripts/Activate.ps1` and then run the commands. Poetry has been configured already and runs perfectly, you just have to activate it if you run into import isssues. 